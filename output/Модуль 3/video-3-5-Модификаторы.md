# Автоматизация Бизнес-процессов. Часть3. БПвБ24-2-part5

Есть еще одна возможность по работе с данными в параметрах действий.

Давайте я снова сюда вставлю уведомления, сообщения соцсети и прочее, прерывание процесса.

Таким вот образом.

Получатель сообщения у нас будет с вами кем создан.

И текст сообщения давайте тоже выберу кем создан.

А теперь с вами посмотрим в документации, что такое модификаторы.

Также пойдем в поддержку 24.

Модификаторы.

Форма такого значения.

Да, у нас текст.

Это статья.

И здесь описано о том, что разные переменные, разные значения, они разных типов.

И к примеру, createdby это вообще-то, скорее всего, ID пользователя.

Это не какой-то текст.

Для того, чтобы нам его в удобоваримом увидеть в текстовом сообщении, нам нужно применить модификатор.

Здесь также много различных возможностей.

Но по большому счету для того, чтобы переводить в читаемые значения, нам нужен модификатор printable.

И для переменной мы можем вызвать его таким вот образом.

Для того, чтобы было наглядно, давайте сверху оставим как есть.

Сохраним, применим и еще раз перезапустим.

Перезапустим.

Вот вы и посмотрите, как у нас отобразились два этих значения


То есть первое это вообще-то малоинформативное сообщение для пользователя.

Это универсистемная переменная, которая используется.

А это уже читаемое значение.

Кто этот самый пользователь.

Тоже еще одна хорошая возможность, которая позволяет реализовывать различные сценарии в бизнес-процессах.

Итак, мы с вами посмотрели, как работать с действием.

Это, собственно, основным элементом вообще при создании бизнес-процессов.

Как видите, возможностей здесь довольно-таки много.